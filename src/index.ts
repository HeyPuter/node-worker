import fs from "./fs"
import { events, stream, buffer, path, util, zlib } from "./node"
import { setPuterAuth } from "./puter"

export default {
	events,
	stream,
	buffer,
	path,
	util,
	zlib,
	fs,
	setPuterAuth,
}
